# ResNeXt 项目核心依赖配置
# 快速安装：pip install -r requirements.txt
# GPU版本：python environment/install_pytorch.py

# 核心深度学习框架
torch>=1.10.0                 # PyTorch深度学习框架
torchvision>=0.11.0          # 计算机视觉库（数据变换等）

# 必需工具库
tqdm>=4.62.0                 # 进度条显示
Pillow>=8.3.0                # 图像处理库
numpy>=1.21.0                # 数值计算库
matplotlib>=3.5.0            # 图表绘制（训练曲线、评估报告）
seaborn>=0.12.0              # 统计可视化（混淆矩阵热力图）

# ============================================================
# ResNeXt 项目核心依赖配置
# ============================================================
# 
# 【快速安装】
# 1. CPU 版本（通用，但速度慢）：
#    pip install -r requirements.txt
#
# 2. GPU 版本（需要 NVIDIA 显卡和 CUDA）：
#    方案 A - 自动安装（推荐）：
#    python install_pytorch.py
#
#    方案 B - 手动指定 CUDA 版本：
#    pip install torch torchvision --index-url https://download.pytorch.org/whl/cu118
#      （将 cu118 替换为你的 CUDA 版本：cu118/cu121/cu124）
#
# 【CUDA 版本查询】
# Windows/Linux：
#   nvidia-smi        # 查看 CUDA Version
# 
# Mac：
#   不支持 NVIDIA CUDA（可使用 Metal Performance Shaders）
#
# ============================================================
# 核心深度学习框架（必须）
# ============================================================

# PyTorch 深度学习框架
# 注：该依赖仅指定基础版本，GPU 加速版本需特别安装
# CPU 版本：torch >= 1.10.0
# GPU 版本：torch >= 1.10.0（cu118/cu121/cu124）
# 官方文档：https://pytorch.org/get-started/locally/
torch>=1.10.0

# PyTorch 官方视觉库（图像处理、数据变换）
torchvision>=0.11.0

# ============================================================
# 必需依赖（核心功能）
# ============================================================

# 进度条显示库
tqdm>=4.62.0

# Python 图像处理库（图片读取和基本操作）
Pillow>=8.3.0

# ============================================================
# 可选依赖（增强功能，不影响核心训练）
# ============================================================

# 数据可视化库（绘制训练曲线、显示图片等）
# 取消下行注释安装：matplotlib
# matplotlib>=3.4.0

# 科学计算库（某些高级功能可能需要）
# 取消下行注释安装：numpy
# numpy>=1.19.0

